services:
  - type: web
    name: ig-playwright-agent
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_VERSION
        value: 20.20.2
      - key: HEADLESS
        value: "true"
      - key: PORT
        value: "3000"
