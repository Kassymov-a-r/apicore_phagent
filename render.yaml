services:
  - type: web
    name: ig-remote-browser-agent
    env: docker
    plan: starter
    disk:
      name: ig-agent-storage
      mountPath: /app/storage
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: STORAGE_DIR
        value: /app/storage
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: /ms-playwright
      - key: OPENAI_API_KEY
        sync: false
